\\Tarea Diagrama de Flujo 
\\Alejandra Bonilla

#include<iostream>
using namespace std; 
int main(){
  int s=12; 
  int p=0 
  cout<<"Dame un numero entero"<<endl; 
  cin>>p; 
  
  if(p is even)
    {
      printf(p=p+1); 
  else 
      if (p is prime) 
          {
            if(p<s) 
              {while(p<s)
               {s=s-p; 
                p=p+2; 
                return p is prime;  
            else
              printf(s=s-1);
         else
          printf(p=p+2);
          return p is prime; 
        if(s=0)
          {cout<<p<<endl;
        else 
          printf(p=p+2);
          return p is prime;}
 return 0;} 
        
     
        
    
